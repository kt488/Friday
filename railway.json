{
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "gunicorn interface.api:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - --error-logfile -",
  "healthcheckPath": "/status"
}
