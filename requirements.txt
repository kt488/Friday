requests
python-dotenv
flask
python-telegram-bot
beautifulsoup4
rich
prompt_toolkit
openpyxl
PyPDF2
pyjwt
cryptography
flask-cors
gunicorn==22.0.0

# ── Voice Pipeline ──────────────────────────────────────────────────────
faster-whisper>=1.0.0            # STT via CTranslate2
kokoro>=0.7.0                    # TTS via KPipeline
webrtcvad>=2.0.10                # VAD (WebRTC)
numpy>=1.24.0                    # Audio processing foundation

# Optional: Audio backends (at least one required)
sounddevice>=0.4.6               # Primary audio I/O (PortAudio)
# pyaudio>=0.2.14                # Secondary audio I/O (fallback, platform-dependent)

# Optional: Silero VAD fallback (better accuracy, needs torch)
# torch>=2.0.0                  # ML framework for Silero VAD
# torchaudio>=2.0.0             # Audio utils for Silero VAD

# ── RAG Engine ──────────────────────────────────────────────────────────
# Core RAG dependencies (pure Python + NVIDIA NIM API)
# numpy already listed above (shared with voice pipeline)
# rank_bm25 optional — FTS5 in SQLite handles BM25 natively
# Scikit-learn optional — only if using local embedding fallback
# scikit-learn>=1.3.0
